export const metadata = {
  title: "Portfolio Details",
  description: "Portfolio page",
};

function PortfolioDetailsLayout({ children }) {
  return <div>{children}</div>;
}

export default PortfolioDetailsLayout;
